# Build results
bin/
obj/

# Visual Studio / Rider / VS Code
.vs/
.vscode/
*.user
*.suo

# User secrets are stored outside the repo by design (%APPDATA%/Microsoft/UserSecrets),
# but never allow a stray one to be committed if ever copied into the repo tree.
**/secrets.json

# Test results
TestResults/
